spring:
  datasource:
    url: jdbc:mysql://localhost:3306/video_auth
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true

security:
  jwt:
    secret: "change-me-super-secret-key"
    issuer: "auth-service"
